spring:
  datasource:
    url: jdbc:mysql://localhost:3307/test
    username: user
    password: testpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate.ddl-auto:
      create-drop
    # JPA 쿼리 로그 출력 설정
    show-sql: true
    properties:
      hibernate:
        format_sql: true
