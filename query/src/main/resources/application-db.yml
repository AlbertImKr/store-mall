# DB
spring:
  datasource:
    url: jdbc:mysql://localhost:3309/commerce
    username: user
    password: testpassword
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate.ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true

